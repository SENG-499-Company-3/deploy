version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/backend
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 0644
hooks:
  BeforeInstall:
    - location: dummyScript2.sh
      timeout: 10
      runas: ubuntu
  AfterInstall:
    - location: dummyScript.sh
      timeout: 30
      runas: ubuntu